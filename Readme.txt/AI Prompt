/*soundBtn.addEventListener("click", () => {
  console.log("clicked", audio.paused);
  if (audio.paused) {
    audio.muted = false;
    audio2.muted = false;
    audio3.muted = false;
    soundIcon.src = "./img/unmuteSound.png";
  } else {
    audio.muted = true;
    audio2.muted = true;
    audio3.muted = true;
    soundIcon.src = "./img/muteSound.png";
  }
});

the above code works well with mute the sound but when i click again , it doesn't unmute ! tell me what is the problem ? in simple JS

*/

/*
function eatBonusTarget() {
  bonusTargetArr.forEach((bonusTargetObj, index) => {
    let isColliding = collectionCheck(dinoObj, bonusTargetObj);
    if (isColliding === true) {
      // if good target touch the dino , will be destroyed
      bonusTargetObj.bonusTargetNode.remove();
      bonusTargetArr.splice(index, 1);
       audio4.play();
      
      // maximize score by 5
      score += 5;
      scoreBtn.textContent = `Score: ${score}`;
    } // else if not colllide with dino Object after x time , it will be destroyed, and dissapeared 
    else if (timer === timer-10) {
      // if good target touch the screen , will be destroyed
      bonusTargetObj.bonusTargetNode.remove();
      bonusTargetArr.splice(index, 1);
      bonusTargetHide();
    }
  });
}

the above code check collision with dino and bonus target , 
when it is collide it is working and destroy and remove but else if i didn't collide , 
i want after x time it will dissapear from screen and be destroyed 
and the above code has problem in this part please tell me what is wrong ?
*/


/*i will give you an example ,
 when dino object touch a good fish object the score increased
  i want in that second appear +1 gif or anything and then disappeared ,
   is the pop up good thinking or you have another solution ?
*/